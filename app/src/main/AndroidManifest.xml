<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.ljmu.cwk2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/die"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DiceGame">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlayScreen" />
        <activity android:name=".HighScreen" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".UserScreen" />
        <activity android:name=".RollItOnTopPlay" />
    </application>
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />
</manifest>